# План side project — SP-SDD-0.3

Работа этого чата явно возобновлена пользователем только для framework-reproducibility. Глобальную очередь не переключаем: CONTINUATION_SCOPE.md. Эталон пакета — F4.95, baseline/hash в BASELINE.json. Готовность: draft / NOT READY FOR SCORED RECONSTRUCTION.

## Завершённый bounded batch

- Сверены F4.92 и F4.95, 36 changed files; 4 targeted rebase checks PASS.
- Пересозданы current inventories/public projections и 3 generator examples на F4.95.
- SQL/bundle owner probes: 16 PASS; Java/OpenAPI evidence и identity vectors получены.
- Diagnostic Runner → Core/KLC subprocesses → neutral publisher/importer → ASGI API: 50 checks PASS.
- 14 HTTP replies, 2 KLC manifests, profile/catalog прошли schema validation.
- Проверены duplicate publication, две revisions, старая pinned revision и read без producer paths.
- 34 offline tests PASS; это не DeepSeek acceptance и не full Framework regression.
- S10 остаётся открытым; S11 подтверждён как различие endpoint projections.
- Framework code/rules/current plan и другие продукты не изменены.

## Следующий bounded batch — SP-SDD-0.4

1. Восстановить checkpoint и pinned F4.95; текущие Drive rules прочитать полностью. Не принимать чужой global active stream за задачу данного явно выбранного side project.
2. Закрыть nested envelope schemas, оставшиеся source/parent IDs и MUST → assertion mapping. Один exact vector не заменяет полный алгоритм.
3. Выполнить high-level KCP planning/execution/publication adapter на открытом corpus; diagnostic author-selected plan не является KCP authorization.
4. Проверить declared object → fields/evidence consumer, пустую JOIN страницу, invalid bundle/base/dependency и atomic rejection. Production entrypoint проверить отдельно от ASGI test client.
5. Подготовить независимый held-out corpus; зафиксировать equivalence и unresolved S10 policy до scored run.
6. Пройти learner-only clean-install/readiness audit. Затем выбрать latest public DeepSeek по официальным источникам на дату запуска, закрепить model/provider/config/budgets.
7. Запустить чистый specification-only эксперимент только при readiness PASS. Корпоративные исходники, Framework source и reviewer материалы модели не передавать.
8. Классифицировать failures, один согласованный цикл улучшения и повтор; затем отдельное решение о расширении до всех live capabilities и eventual merge.

## Среда и границы

Python 3.12.14, 43 third-party distributions в изолированном каталоге; runtime-requirements.txt фиксирует реально использованные версии, но не universal platform lockfile. Команды README используют штатных owners неизменённого reference source.

Не собирать deliveries/recovery и не исправлять Framework автоматически. Все результаты этого потока сохраняются отдельно. Полный охват всего live Framework остаётся целью; пилот не сокращает её.
# Точка продолжения SP-SDD-0.4

Сначала читать CURRENT_REFERENCE.md: текущий reference F4.103, legacy vectors F4.95.
Порядок следующего приращения: обновить выбранные learner projections; описать
nested SQL/identity; закрыть transport atomic negatives; проверить learner-only
install; затем скрытый acceptance. Старые формулировки полного KCP/TCP gap ниже
уточнены текущим production report. Не расширять работу на Framework.
